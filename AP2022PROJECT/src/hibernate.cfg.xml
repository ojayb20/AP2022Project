<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE hibernate-configuration SYSTEM "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd" PUBLIC "-//Hibernate/Hiberante Configuration DTD 3.0//EN">

-<hibernate-configuration>
    -<session-factory>

        <!-- JDBC files.database connection settings -->


        <property name="connection.driver_class">com.mysql.jdbc.Driver</property>

        <property name="connection.password">password</property>

        <property name="connection.url">jdbc:mysql://localhost:3306/projectdb</property>

        <property name="connection.username">root</property>

        <property name="connection.password"> </property>

        <!-- JDBC Connection Pool Setting -->


        <property name="connection.pool_size">1</property>

        <!-- Choose the Hibernate dialect for the specific files.database type -->


        <property name="hibernate.dialect">org.hibernate.dialect.MySQLDialect</property>

        <!-- Echo out the SQL to the console -->


        <property name="hibernate.show_sql">true</property>

        <property name="current_session_context_class">thread</property>

    </session-factory>
</hibernate-configuration>